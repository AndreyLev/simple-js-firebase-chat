if ((window.matchMedia('(max-width: 521px')).matches) {
    // set placeholder as "Enter the name"

    nameForm.name.placeholder = "Введите имя";
    newChatForm.message.placeholder = "Введите сообщение"
    // set placeholder as "Enter the message"
}